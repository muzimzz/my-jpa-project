spring:
  application:
    name: review
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jpa_start?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: test1234

  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true